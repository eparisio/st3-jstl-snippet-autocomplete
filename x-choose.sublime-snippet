<snippet>
	<content><![CDATA[
<x:choose>
   <x:when select="${1}">
      ${2}
   </x:when>
   <x:when select="${3}">
      ${4}
   </x:when>
   <x:otherwise>
      ${5}
   </x:otherwise>
</x:choose>
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>x:choose</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>text.html.jsp</scope>
</snippet>